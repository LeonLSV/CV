import styled from "styled-components";

export const EstudiosGloabl = styled.section`
  width: 60px;
  transform: rotate(-90deg);
  font-size: 12px;
  color: black;
  text-align: center;
`;

export const GridEstudios = styled.div`
  display: grid;
  grid-template-columns: 0.1fr 0.1fr 1fr;
  grid-template-rows: 1fr 1fr;

  margin: auto 100px;
`;
